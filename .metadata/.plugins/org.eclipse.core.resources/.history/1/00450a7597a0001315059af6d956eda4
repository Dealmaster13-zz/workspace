package uk.ac.cam.oda22.coverage;

import uk.ac.cam.oda22.core.environment.IRoom;
import uk.ac.cam.oda22.core.environment.PolygonRoom;
import uk.ac.cam.oda22.core.logging.Log;
import uk.ac.cam.oda22.core.robots.Robot;

/**
 * @author Oliver
 * 
 */
public class SimpleCoverage implements ICoverage {

	@Override
	public CoverageResult performCoverage(IRoom room, Robot robot) {
		if (!(room instanceof PolygonRoom)) {
			Log.error("Coverage unimplemented for non-polygonal rooms");

			return null;
		}

		PolygonRoom pRoom = (PolygonRoom) room;

		return null;
	}

}
