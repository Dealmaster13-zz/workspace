package uk.ac.cam.oda22.core.environment;

/**
 * @author Oliver
 *
 */
public enum NodeVisibility {

	FULLY_VISIBLE,
	
	ALONG_OBSTACLE_EDGE,
	
	SAME_POINT,
	
	NOT_VISIBLE;
	
	public boolean partlyVisible(NodeVisibility v) {
		return v == FULLY_VISIBLE || v == ALONG_OBSTACLE_EDGE;
	}
	
}
